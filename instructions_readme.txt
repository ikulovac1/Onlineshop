 - Make database called auction (run the commands in sql.txt).

 - In db.js make changes for your db.

 - install all dependencies for project (nodemon, babel, axios, etc)

 - all dependencies can be found in two package.json (first in "/"; second in "/auction".

 - open two terminals. In one (root folder) run "npm start", and in second (/auction folder) run "npm start"

 - first run the command in root folder (node) then second (react).

 - Dont forget to kill servers!